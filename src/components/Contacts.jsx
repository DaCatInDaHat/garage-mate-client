import { Typography } from '@mui/material'
import React from 'react'

const Contacts = () => {
    return (
        <Typography variant='h2'>Contacts component in development</Typography>
    )
}

export default Contacts